---
const today = new Date();

interface Props {
	companyName?: string;
	rightsLabel?: string;
}

const { companyName = 'SierraWalks', rightsLabel = 'All rights reserved.' } = Astro.props;
---

<footer class="site-footer">
	<p>&copy; {today.getFullYear()} {companyName}. {rightsLabel}</p>
</footer>
